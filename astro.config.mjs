// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Production origin — drives canonical URLs and Open Graph tags.
  site: 'https://www.microaptech.com',
});
